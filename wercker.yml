box: hashexpression/wercker
build:
  # Steps make up the actions in your pipeline
  # Read more about steps on our dev center:
  # https://devcenter.wercker.com/development/steps/
  steps:
    - script:
        name: build & test
        code: |
          echo "hello world"
#          stack build --test --ghc-options -O2


create-release:
  steps:
    - script:
        name: Get symphony release version
        code: export SYMPHONY_RELEASE_VERSION=$(cat .symphony_version)
    - wercker/github-create-release@2.1.1:
        token: $GITHUB_TOKEN
        tag: $SYMPHONY_RELEASE_VERSION

